# Exactly pinned local verification toolchain. The PowerFactory runtime itself
# uses only the standard library and PowerFactory's built-in module.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/pytest -q
pytest==9.1.1
# Pinned transitive pytest dependencies for reproducible verification.
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
